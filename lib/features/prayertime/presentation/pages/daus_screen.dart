import 'package:flutter/material.dart';

class DausScreen extends StatelessWidget {
  const DausScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
        body: Center(
      child: Text('Daus Screen'),
    ));
  }
}
